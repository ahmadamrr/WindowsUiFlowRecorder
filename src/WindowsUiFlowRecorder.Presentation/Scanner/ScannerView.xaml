<UserControl x:Class="WindowsUiFlowRecorder.Presentation.Scanner.ScannerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:scanner="clr-namespace:WindowsUiFlowRecorder.Presentation.Scanner"
             xmlns:converters="clr-namespace:WindowsUiFlowRecorder.Presentation.Shared.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>

        <Style x:Key="TreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="{Binding IsExpanded}"/>
            <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
            <Setter Property="Visibility" Value="{Binding Visibility}"/>
            <Setter Property="Foreground" Value="#CCC"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#264F78"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" Padding="10" CornerRadius="4">
            <TextBlock Text="Smart UI Scanner" FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>

        <Border Grid.Row="1" Margin="0,8,0,0" Background="#1E1E1E" Padding="8" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" ItemsSource="{Binding Processes}"
                          SelectedItem="{Binding SelectedProcess}"
                          DisplayMemberPath="DisplayText"
                          Background="#333" Foreground="#CCC"
                          Margin="0,0,8,0" Padding="4" IsReadOnly="True"/>

                <Button Grid.Column="1" Content="⟳ Refresh"
                        Command="{Binding RefreshProcessesCommand}"
                        Background="#555" Foreground="White" BorderThickness="0"
                        Padding="8,4" Margin="4,0"/>

                <Button Grid.Column="2" Content="Scan"
                        Command="{Binding ScanCommand}"
                        Background="#007ACC" Foreground="White" BorderThickness="0"
                        Padding="12,4" Margin="4,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#444" BorderThickness="1" CornerRadius="4"
                    Background="#1A1A1A" Padding="8" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,8" Padding="6,4"
                             Background="#333" Foreground="#CCC" BorderThickness="0" FontSize="12"
                             Visibility="{Binding HasScanResult, Converter={StaticResource BoolToVis}}"/>

                    <TreeView Grid.Row="1" x:Name="ElementTree"
                              ItemsSource="{Binding TreeRoots}"
                              ItemContainerStyle="{StaticResource TreeViewItemStyle}"
                              Background="#1E1E1E" BorderThickness="0"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              SelectedItemChanged="OnTreeSelectionChanged"
                              Visibility="{Binding HasScanResult, Converter={StaticResource BoolToVis}}">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type scanner:ElementTreeNode}"
                                                      ItemsSource="{Binding Children}">
                                <TextBlock Text="{Binding DisplayText}" FontSize="12" FontFamily="Consolas"
                                           Foreground="#AAA"/>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>

                    <TextBlock Grid.Row="1" Text="{Binding StatusMessage}"
                               Foreground="#666" FontSize="13"
                               VerticalAlignment="Center" HorizontalAlignment="Center"
                               Visibility="{Binding HasScanResult, Converter={StaticResource InverseBoolToVis}}"/>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="4" Background="#333" Margin="0"/>

            <Border Grid.Column="2" BorderBrush="#444" BorderThickness="1" CornerRadius="4"
                    Background="#1A1A1A" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Element Details" FontSize="14"
                               FontWeight="SemiBold" Foreground="#CCC" Margin="0,0,0,8"/>

                    <Border Grid.Row="1" Background="#333" Padding="8" CornerRadius="4"
                            Margin="0,0,0,8">
                        <TextBlock Foreground="#888" FontSize="11" FontFamily="Consolas">
                            <Run Text="Position: "/>
                            <Run Text="{Binding HighlightBounds.X}"/><Run Text=", "/>
                            <Run Text="{Binding HighlightBounds.Y}"/><Run Text="  "/>
                            <Run Text="{Binding HighlightBounds.Width}"/><Run Text="x"/>
                            <Run Text="{Binding HighlightBounds.Height}"/>
                        </TextBlock>
                    </Border>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{Binding ElementDetails}" Foreground="#AAA"
                                   FontSize="12" FontFamily="Consolas" TextWrapping="Wrap"/>
                    </ScrollViewer>

                    <TextBlock Grid.Row="2" Text="Select an element in the tree to view details"
                               Foreground="#555" FontSize="12" VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Visibility="{Binding HasSelection, Converter={StaticResource InverseBoolToVis}}"/>

                    <Button Grid.Row="3" Content="Export Scan"
                            Command="{Binding ExportCommand}"
                            Background="#28A745" Foreground="White" BorderThickness="0"
                            Padding="12,6" Margin="0,8,0,0" FontSize="13"
                            Visibility="{Binding HasScanResult, Converter={StaticResource BoolToVis}}"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="#007ACC" Padding="8" CornerRadius="4" Margin="0,8,0,0">
            <TextBlock Text="{Binding StatusMessage, StringFormat='Status: {0}'}" Foreground="White" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>